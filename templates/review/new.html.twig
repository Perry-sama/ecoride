{% extends 'base.html.twig' %}

{% block title %}Ajouter un avis{% endblock %}

{% block body %}
<h1>Ajouter un avis pour le trajet {{ trip.depart }} → {{ trip.destination }}</h1>

{{ form_start(form) }}
    {{ form_row(form.content) }}
    {# Pas besoin de statut ici, le form le met en pending automatiquement #}
    <button class="btn btn-primary">Envoyer</button>
{{ form_end(form) }}
{% endblock %}
