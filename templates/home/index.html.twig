{% extends 'base.html.twig' %}

{% block title %}
  <img src="logo/logoEcoRide.png" alt="EcoRide logo" class="my-3">
  Accueil - EcoRide
{% endblock %}

{% block body %}
<section class="text-center mb-5">
  <h1 class="display-4 text-success">Bienvenue sur <strong>EcoRide</strong> 🌿</h1>
  <p class="lead">Le covoiturage éco-responsable, simple et humain.</p>
</section>

<section class="mb-5">
  {{ form_start(form) }}
    <div class="row justify-content-center">
      <div class="col-md-6">
        {{ form_row(form.depart) }}
        {{ form_row(form.arrival) }}
        <button type="submit" class="btn btn-success mt-3" disabled>Rechercher (bientôt dispo)</button>
      </div>
    </div>
  {{ form_end(form) }}
</section>

<section class="mb-5">
  <h2 class="text-center text-success">🚗 Trajets disponibles</h2>
  <div class="row justify-content-center">
    {% for trajet in trajets %}
      <div class="col-md-5 my-3">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title text-success">{{ trajet.depart }} → {{ trajet.destination }}</h5>
            <p class="card-text">
              <strong>Date :</strong> {{ trajet.dateHeure|date('d/m/Y H:i') }}<br>
              <strong>Places :</strong> {{ trajet.nbPlaces }}<br>
              <strong>Prix :</strong> {{ trajet.prix }} €<br>
              <strong>Description :</strong> {{ trajet.description|default('—') }}<br>
              <strong>Conducteur :</strong> {{ trajet.user.email }}
            </p>
            <a href="{{ path('app_reservation_new', { id: trajet.id }) }}" class="btn btn-primary">Réserver ce trajet</a>
          </div>
        </div>
      </div>
    {% else %}
      <p class="text-center">Aucun trajet disponible pour le moment.</p>
    {% endfor %}
  </div>
</section>

<section class="row justify-content-center text-center mb-5">
  <article class="col-md-4">
    <h2 class="h4 text-success">🌍 Respect de la planète</h2>
    <p>Moins de voitures, moins de CO₂. C’est notre mission !</p>
  </article>
  <article class="col-md-4">
    <h2 class="h4 text-success">🚗 Trajets partagés</h2>
    <p>Créez ou rejoignez un trajet en quelques clics.</p>
  </article>
  <article class="col-md-4">
    <h2 class="h4 text-success">🤝 Communauté solidaire</h2>
    <p>Des trajets éthiques entre gens bienveillants.</p>
  </article>
</section>
{% endblock %}
