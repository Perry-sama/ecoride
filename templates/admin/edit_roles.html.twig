{% extends 'base.html.twig' %}

{% block title %}Modifier les rôles de {{ user.email }}{% endblock %}

{% block body %}
<section class="container mt-5">
    <h1 class="mb-4">Modifier les rôles de l’utilisateur <strong>{{ user.email }}</strong></h1>

    {{ form_start(form) }}
        {{ form_row(form.roles) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}
</section>
{% endblock %}

