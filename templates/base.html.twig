<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}EcoRide - Covoiturage écologique{% endblock %}</title>

    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block stylesheets %}
    <style>
      body {
        background-color: #f5fff5;
      }
      header, footer {
        background-color: #d4edda;
      }
      .nav-link {
        color: #155724 !important;
      }
      .logo {
        font-weight: bold;
        font-size: 1.5rem;
        color: #155724;
      }
    </style>
    {% endblock %}
  </head>

  <body>
    <!-- HEADER + NAV -->
    <header class="p-3 shadow-sm mb-4">
      <div class="container d-flex flex-wrap align-items-center justify-content-between">
        <a href="{{ path('app_home') }}" class="logo text-decoration-none">EcoRide 🌱</a>

        <nav class="nav">
          {% if app.user %}
            <a class="nav-link" href="{{ path('trajet_index') }}">Mes trajets</a>
            <a class="nav-link" href="{{ path('app_profile') }}">Profil</a>
            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
          {% else %}
            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
          {% endif %}
        </nav>
      </div>
    </header>

    <!-- CONTENU PRINCIPAL -->
    <main class="container">
      {% block body %}{% endblock %}
    </main>

    <!-- FOOTER -->
    <footer class="mt-5 p-4 text-center text-muted small">
      <p>Contact : contact@ecoride.fr | <a href="{{ path('app_mentions_legales') }}">Mentions Légales</a></p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
  </body>
</html>
