<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}EcoRide{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>
    <header class="p-3 bg-dark text-white">
        <nav class="container d-flex justify-content-between align-items-center">
            <a href="{{ path('app_home') }}" class="text-white text-decoration-none">EcoRide</a>

            <div>
                {% if app.user %}
                    <span class="me-2">Bienvenue, {{ app.user.firstname }}</span>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-light btn-sm">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-light btn-sm">Connexion</a>
                {% endif %}
            </div>
        </nav>
    </header>

    <main class="container mt-4">
        {% block body %}{% endblock %}
    </main>

    <footer class="text-center mt-5 p-3 bg-light">
        <small>© EcoRide {{ "now"|date("Y") }}</small>
    </footer>
</body>
</html>
