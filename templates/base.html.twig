{# templates/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}EcoRide - Covoiturage écolo{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #e6f2e6;
            font-family: 'Segoe UI', sans-serif;
        }
        header {
            background-color: #2e7d32;
            color: white;
        }
        footer {
            background-color: #1b5e20;
            color: white;
        }
    </style>
</head>
<body>
    <header class="py-3 mb-4">
        <div class="container d-flex justify-content-between align-items-center">
            <h1 class="h3">🌱 EcoRide</h1>
            <nav>
                <a href="{{ path('app_home') }}" class="text-white me-3">Accueil</a>
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="text-white">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="text-white me-3">Connexion</a>
                    <a href="{{ path('app_register') }}" class="text-white">Inscription</a>
                {% endif %}
            </nav>
        </div>
    </header>

    <main class="container my-4">
        {% block body %}{% endblock %}
    </main>

    <footer class="py-4 mt-5 text-center">
        <p>Contact : contact@ecoride.fr</p>
        <p><a href="{{ path('app_mentions_legales') }}" class="text-white">Mentions légales</a></p>
    </footer>
</body>
</html>
