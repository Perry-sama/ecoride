{% extends 'base.html.twig' %}

{% block title %}Réserver un trajet{% endblock %}

{% block body %}
<main class="container mt-4">
    <h2>📝 Réserver une place</h2>

    <div class="card mt-3">
        <div class="card-header bg-light">
            Trajet : {{ trajet.depart }} → {{ trajet.destination }} le {{ trajet.dateHeure|date('d/m/Y H:i') }}
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.places) }}
                    {{ form_widget(form.places, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.places) }}
                </div>
                <button class="btn btn-success">Réserver</button>
            {{ form_end(form) }}
        </div>
    </div>

    <footer class="mt-4">
        <a href="{{ path('app_trajet_show', {id: trajet.id}) }}" class="btn btn-outline-secondary">⬅️ Retour au trajet</a>
    </footer>
</main>
{% endblock %}
