{% extends 'base.html.twig' %}

{% block title %}Détail de la réservation{% endblock %}

{% block body %}
<main class="container mt-4">
    <h2>🎫 Réservation</h2>

    <div class="card mt-3">
        <div class="card-body">
            <p><strong>Trajet :</strong> {{ reservation.trajet.depart }} → {{ reservation.trajet.destination }}</p>
            <p><strong>Date :</strong> {{ reservation.trajet.dateHeure|date('d/m/Y H:i') }}</p>
            <p><strong>Places réservées :</strong> {{ reservation.places }}</p>
            <p><strong>Réservé le :</strong> {{ reservation.createdAt|date('d/m/Y H:i') }}</p>
        </div>
    </div>

    <form method="post" action="{{ path('app_reservation_delete', {'id': reservation.id}) }}" class="mt-3"
          onsubmit="return confirm('Voulez-vous vraiment annuler cette réservation ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
        <button class="btn btn-danger">🗑️ Annuler la réservation</button>
    </form>

    <footer class="mt-4">
        <a href="{{ path('app_reservation_index') }}" class="btn btn-outline-secondary">⬅️ Retour à mes réservations</a>
    </footer>
</main>
{% endblock %}
